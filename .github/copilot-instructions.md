## Copilot / agent instructions

See root `AGENTS.md` and `CLAUDE.md`.

Claimgate verifies agent claims with independent gates. Prefer fixing adapters and policy in `packages/` rather than bypassing checks in examples.
