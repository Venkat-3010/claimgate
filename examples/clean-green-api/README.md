# Clean green API

A small TypeScript order API with Vitest + Zod env schema.

**Expected:** `claimgate verify` **PASS**

```bash
pnpm install && pnpm build
cd examples/clean-green-api
pnpm verify
```
