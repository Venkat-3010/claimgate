# Synced Drizzle orders

SQLite Drizzle schema with an initial migration + Vitest helpers.

**Expected:** `claimgate verify` **PASS**

```bash
pnpm install && pnpm build
cd examples/synced-drizzle-orders
pnpm verify
```
