# Demo assets

- [`demo.svg`](./demo.svg) — side-by-side PASS vs FAIL terminal demo (wired in the root README).
- Regenerate: `node scripts/generate-demo-svg.mjs`

Optional: drop a recorded GIF as `demo.gif` later; keep the SVG as the default hero so the README stays crisp without binary churn.
